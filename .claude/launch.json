{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "booking-tracker",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver; p=int(os.environ.get('PORT',8000)); httpd=socketserver.TCPServer(('',p),http.server.SimpleHTTPRequestHandler); print(f'Serving on {p}',flush=True); httpd.serve_forever()"],
      "autoPort": true
    }
  ]
}
